[tox]
envlist = py37

[base]
deps =
    -rdev-requirements.txt
    -rrequirements.txt

[testenv]
passenv = TRAVIS TRAVIS_*
deps =
    {[base]deps}
commands =
    coverage run -m pytest {posargs}
    coverage report
    coverage xml
