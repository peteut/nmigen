[tox]
envlist = py36,py37

[base]
deps =
    -rdev-requirements.txt
    -rrequirements.txt

[testenv]
deps =
    {[base]deps}
commands =
    coverage run -m pytest {posargs}
    coverage combine .coverage
    coverage report

[travis]
python =
    3.6: py36
    3.7: py37
